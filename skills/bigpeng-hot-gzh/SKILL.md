---
name: bigpeng-hot-gzh
description: >-
  为微信公众号生成爆款选题方向和候选标题。有明确选题、文章描述、草稿或草稿标题时，输出多条不同公式的候选标题；只有关键词、简单想法或未给输入时，先给选题方向再配标题。
  用于用户提到爆款标题、公众号标题、起标题、选题、不知道写什么、候选标题、公众号选题、爆款公众号时。
  只做选题和标题，不写正文。
---

# 爆款公众号选题与标题

本 skill 由 BigPeng 维护。公式来自 AI 工具/效率赛道的爆款公号标题，换赛道时复用槽位，不要复用语料里的产品名。

只做选题和标题，不写正文。写正文交给其他写作 skill。本 skill 也不替代推文短标题。

## 先读这些参考

按任务读取，不要一次塞满：

- `references/topic-templates.md`：8 个选题模板、何时用、正文要兑现什么。路径 B 必读。
- `references/title-formulas.md`：7 型公式、长度、补元素规则、禁用词。路径 A/B 都读。
- `references/title-corpus.md`：真实标题样本，只校准句式。
- `references/qa-checklist.md`：发出前检查。

## 路由

先判断走哪条路，不确定时用一句话确认，不要两条都跑。

| 用户给了什么 | 路径 |
|---|---|
| 文章描述、草稿、草稿标题、明确主题句，或说「帮我起标题 / 做几个爆款标题」 | **A** 只出标题 |
| 关键词、一句话想法、「最近写点什么」、空输入 | **B** 先选题后标题 |

「帮我把这篇起标题」即使只有几句话，也走 A。
「想写点 Codex / 不知道写什么」走 B。

## 路径 A：已有选题 → 候选标题

1. 读 `references/title-formulas.md`。需要校准句式时再读 `references/title-corpus.md`。
2. 抽出可钩元素：对象工具、身份、数字、结果、冲突、代价、附赠物。缺数字或身份时，按公式文档**合理补全并标注需兑现**；不编造 Star、下载量、实验样本量。
3. 覆盖 **6 个不同公式**，默认 6 条。用户要更多再加。情绪短句最多 1 条。
4. 过 `references/qa-checklist.md`。
5. 按下面格式输出，标 1 个首选。

### 路径 A 输出

```markdown
## 选题理解
（一句话：给谁看、讲什么对象、核心结果是什么）

## 候选标题

1. **标题**
   - 公式：
   - 字数：
   - 为何可能点：
   - 兑现：正文必须提供什么

2. ...（共 6 条）

## 首选
**标题**
（一句：截断后仍成立 / 有数字或身份 / 能兑现）
```

## 路径 B：无明确选题 → 方向 + 标题

1. 读 `references/topic-templates.md` 和 `references/title-formulas.md`。
2. **空输入**：用当前日期，WebSearch 找 1 个可写的新工具或新事件，再套模板。不要只从语料抄过时产品名。搜不到热点时，用「身份场景化」或「反常识」出可复用方向，并说明未绑死某个新品。
3. **有关键词**：把词映射到 8 个模板里最匹配的方向。默认 **4 个方向**，模板不重复。
4. 每个方向给 3 个候选标题，公式不重复。
5. 过 `references/qa-checklist.md`。
6. 最后给 1 个「最值得先写」。

### 路径 B 输出

```markdown
## 输入理解
（关键词 / 空输入时用了哪条当前热点）

## 方向 1：一句话选题
- 模板：
- 为什么现在能写：
- 建议证据：亲测 / 清单 / 对比数据（选具体的）

1. **标题**（公式 / 字数 / 兑现）
2. **标题**（公式 / 字数 / 兑现）
3. **标题**（公式 / 字数 / 兑现）

## 方向 2
...（共 4 个方向）

## 最值得先写
**方向名 + 首选标题**
（时效 × 身份 × 可执行，一句）
```

## 硬规则

- 输出 Markdown，不用 JSON。
- 不要开始写文章、大纲或配图。用户接着说「按这个写」再交出去。
- 标题写了附教程 / 提示词 / 开源仓库，兑现点必须写明对应物从哪来。
- 6 条标题禁止同义改写。
- 通稿体、震惊/重磅/炸裂、空数字，一律不用。
